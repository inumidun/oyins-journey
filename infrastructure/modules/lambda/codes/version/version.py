import json\nimport boto3\nimport os\nimport logging\nfrom typing import Dict, Any, List\nfrom botocore.exceptions import ClientError\nfrom datetime import datetime\n\n# Configure logging\nlogger = logging.getLogger()\nlogger.setLevel(logging.INFO)\n\n# Initialize DynamoDB resource\ndynamodb = boto3.resource('dynamodb')\ntable_name = os.environ.get('VERSIONS_TABLE')\n\nif not table_name:\n    logger.error(\"VERSIONS_TABLE environment variable not set\")\n    raise ValueError(\"VERSIONS_TABLE environment variable is required\")\n\nversions_table = dynamodb.Table(table_name)\n\ndef lambda_handler(event: Dict[str, Any], context: Any) -> Dict[str, Any]:\n    \"\"\"\n    AWS Lambda handler for version API endpoint.\n    \n    Supports:\n    - GET /version - Get current CV version and system info\n    \n    Args:\n        event: API Gateway event\n        context: Lambda context\n        \n    Returns:\n        API Gateway response\n    \"\"\"\n    try:\n        # Log the incoming request\n        logger.info(f\"Processing request: {json.dumps(event, default=str)}\")\n        \n        # Get all versions from database\n        versions = _get_all_versions()\n        \n        # Sort by version number (newest first) if versions exist\n        if versions:\n            versions.sort(key=lambda x: x.get('version_number', '0.0.0'), reverse=True)\n            current_version = versions[0]\n        else:\n            current_version = None\n        \n        # Generate system info dynamically\n        system_info = _get_system_info(context)\n        \n        response_body = {\n            'system_info': system_info\n        }\n        \n        # Only include version data if it exists\n        if current_version:\n            response_body['current_version'] = current_version\n            response_body['version_history'] = versions\n            response_body['total_versions'] = len(versions)\n        else:\n            response_body['message'] = 'No version data available. System is operational.'\n        \n        return {\n            'statusCode': 200,\n            'headers': {\n                'Content-Type': 'application/json',\n                'Access-Control-Allow-Origin': '*',\n                'Access-Control-Allow-Methods': 'GET, OPTIONS',\n                'Access-Control-Allow-Headers': 'Content-Type'\n            },\n            'body': json.dumps(response_body)\n        }\n        \n    except ClientError as e:\n        logger.error(f\"DynamoDB error: {e}\")\n        return _error_response(500, \"Database error occurred\", context)\n    except Exception as e:\n        logger.error(f\"Unexpected error: {e}\")\n        return _error_response(500, \"Internal server error\", context)\n\ndef _get_all_versions() -> List[Dict[str, Any]]:\n    \"\"\"\n    Get all versions from the table.\n    \n    Returns:\n        List of all versions\n    \"\"\"\n    try:\n        response = versions_table.scan()\n        return response.get('Items', [])\n    except ClientError as e:\n        logger.error(f\"Error scanning versions table: {e}\")\n        raise\n\ndef _get_system_info(context: Any) -> Dict[str, Any]:\n    \"\"\"\n    Generate dynamic system information.\n    \n    Args:\n        context: Lambda context\n        \n    Returns:\n        System information dictionary\n    \"\"\"\n    function_name = os.environ.get('AWS_LAMBDA_FUNCTION_NAME', '')\n    \n    # Extract environment from function name\n    environment = 'unknown'\n    if function_name:\n        parts = function_name.split('-')\n        if len(parts) >= 3:\n            environment = parts[2]  # oyins-journey-{env}-version\n    \n    return {\n        'api_version': '1.0.0',\n        'timestamp': datetime.utcnow().isoformat() + 'Z',\n        'environment': environment,\n        'function_name': function_name,\n        'request_id': context.aws_request_id,\n        'region': os.environ.get('AWS_REGION', 'us-east-1'),\n        'runtime': 'python3.13',\n        'architecture': {\n            'compute': 'AWS Lambda',\n            'database': 'DynamoDB',\n            'api': 'API Gateway',\n            'cdn': 'CloudFront',\n            'storage': 'S3',\n            'iac': 'Terraform',\n            'cicd': 'GitHub Actions'\n        }\n    }\n\ndef _error_response(status_code: int, message: str, context: Any) -> Dict[str, Any]:\n    \"\"\"\n    Create standardized error response.\n    \n    Args:\n        status_code: HTTP status code\n        message: Error message\n        context: Lambda context\n        \n    Returns:\n        API Gateway error response\n    \"\"\"\n    return {\n        'statusCode': status_code,\n        'headers': {\n            'Content-Type': 'application/json',\n            'Access-Control-Allow-Origin': '*'\n        },\n        'body': json.dumps({\n            'error': message,\n            'timestamp': datetime.utcnow().isoformat() + 'Z',\n            'request_id': context.aws_request_id\n        })\n    }